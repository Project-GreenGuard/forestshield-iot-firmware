// Configuration file for ESP32 Wildfire Sensor
// Copy this file to config.h and fill in your values
// config.h is in .gitignore and will not be committed

#ifndef CONFIG_H
#define CONFIG_H

// WiFi credentials
const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// AWS IoT Core Configuration
const char* AWS_IOT_ENDPOINT = "YOUR_IOT_ENDPOINT.iot.us-east-1.amazonaws.com";

// Device Information
const char* DEVICE_ID = "esp32-01";
const float SENSOR_LAT = 43.467;  // Update with actual sensor location
const float SENSOR_LNG = -79.699; // Update with actual sensor location

#endif

